#if !defined(MAX)
#define	MAX(A, B)	((A) > (B) ? (A) : (B))
#endif

#if !defined(MIN)
#define	MIN(A, B)	((A) < (B) ? (A) : (B))
#endif

#define DEBUG 	0
#define	SHOW	0
#define SIG_CAP 50
#define MAX_C   10
#define TOL 	1e-6					   
#define PI 	3.14159265358979323846

#include "common.h"
